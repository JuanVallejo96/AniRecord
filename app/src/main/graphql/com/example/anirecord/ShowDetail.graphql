query ShowDetail($showId: Int!) {
    Media(id: $showId) {
        id
        title {
            english
            romaji
        }
        description(asHtml:true)
        coverImage {
            extraLarge
        }
        bannerImage
        averageScore
        episodes
        season
        seasonYear
        nextAiringEpisode {
            airingAt
            episode
            media {
                coverImage {
                    large
                }
            }
        }
    }
}